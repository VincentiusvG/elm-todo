{
  "name": "elm-todo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "elm-live": "elm-live src/Main.elm --open --pushstate --output=elm-todo.js",
    "elm-debug": "elm-live src/Main.elm --open --pushstate --debug --output=elm-todo.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "elm": "^0.18.0",
    "elm-live": "^2.6.0",
    "elm-test": "^0.17.3"
  }
}
